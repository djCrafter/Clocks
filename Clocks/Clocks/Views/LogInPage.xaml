<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Clocks.Views.LogInPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="MockUser1 -- Login: Admin   Pass: 12345" HorizontalTextAlignment="Center"/>
            <Label Text="MockUser2 -- Login: User   Pass: 54321" HorizontalTextAlignment="Center"/>
            <Entry Text="{Binding Login}" Margin="0, 100, 0, 0"/>
            <Entry Text="{Binding Password}" IsPassword="True"/>
            <Button Text="LOG IN" Command="{Binding LogInCommand}"/>
            <Label Text="{Binding Message}" HorizontalTextAlignment="Center" TextColor="Red"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>